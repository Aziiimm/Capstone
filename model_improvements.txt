Ways to improve the recommender (item–item kNN / Capstone pipeline)
================================================================

Use this list as a backlog. Say when to implement items and which numbers you want first.


DATA & SPLITS
-------------
- Use larger / real Amazon slices (full category Parquet) so the model sees more users and items.
- Tune filter_counts thresholds (min reviews per user / per item) for signal vs catalog size.
- Use a time-based train/test split (hold out recent interactions via timestamp) instead of random splits only.


MATRIX / RATINGS
----------------
- User-center or normalize ratings (subtract user mean) before building the sparse matrix.
- Try implicit feedback: binary “interacted or not” or threshold ratings instead of raw stars.


HYPERPARAMETERS
---------------
- Tune n_neighbors in NearestNeighbors / new_model.py (too low = noisy; too high = vague).
- Tune top-k for recommendations separately from graph neighbor count.


RECOMMENDATION LOGIC (CODE)
---------------------------
- Replace the current “ravel + slice” merge of neighbors from multiple liked items with a scored merge:
  aggregate neighbors from each history item (e.g. weight by similarity and/or user rating),
  dedupe items, sort by score, then take top-k.


EVALUATION & BASELINES
----------------------
- Always evaluate on held-out data with no leakage; compare against baselines on the same split:
  - Random top-k items
  - Popularity (most-rated / most-reviewed items)
- Report Hit Rate@K and MRR@K (already in evaluate_ranking.py); add baseline scripts if missing.


STRONGER MODELS (OPTIONAL / LARGER SCOPE)
----------------------------------------
- Matrix factorization (e.g. ALS / implicit ALS) on the same sparse matrix.
- Hybrid: collaborative filtering + content (titles/reviews: TF-IDF or embeddings).


INFRASTRUCTURE
--------------
- GPU (RAPIDS): watch VRAM on 6 GB cards — smaller RMM pool, limits, or chunked training.
- CPU vs GPU: same algorithm should be compared with fair splits and timing, not accuracy alone.


Suggested implementation order for this repo
--------------------------------------------
1) Better neighbor merging + optional user-centering (biggest bang for kNN without new libraries).
2) Temporal split option in evaluate_ranking.py + popularity/random baselines.
3) Hyperparameter sweep (neighbors, top-k) logged to a small results table.
4) Optional ALS or hybrid if course scope allows.

DONE in repo (code + improvement_logs/)
---------------------------------------
• Iteration 01: scored merge + user centering (neighbor_merge.py, recommenders, new_model).
• Iteration 02: temporal split + popularity/random baselines; Parquet directory support.
• Iteration 03: rating-weighted merge row_weights; --implicit-matrix in evaluate_ranking + new_model;
  recommend(..., history_ratings=...).
• Iteration 04: BM25 matrix reweighting (--bm25-weighting); new TruncatedSVD model
  (--model svd --svd-components N) inside evaluate_ranking.py. Composes with BM25.
• Iteration 05: automated sweep (models/sweep_ranking.py → output/sweep_results.csv).
  25 configs × 4 datasets; best model differs by dataset (see improvement_logs/iteration_05_sweep.md).
• Iteration 06: ALS (implicit), kNN+SVD ensemble, popularity prior, query-neighbors,
  min-history-rating, recency decay (ranking_models.py + evaluate_ranking.py).
  Tools best: 32.83% Hit@10 ensemble temporal (see improvement_logs/iteration_06_als_ensemble.md).
• Iteration 07: extended sweep v2 → output/sweep_results_v2.csv (see improvement_logs/iteration_07_sweep_v2.md).
• Iteration 08: content TF-IDF hybrid (content_hybrid.py), 3-model stack, NMF, knn metric,
  ALS confidence alpha; sweep_iteration08.py → output/sweep_iteration08.csv.
• Iteration 09: SVD-96 temporal best on dev_tools (33.00% Hit@10); content 20% helps
  industrial/sample slightly; stack does not beat ensemble on Tools.
  See improvement_logs/iteration_08_content_stack.md, iteration_09_svd_content_tune.md.


Iteration 05 sweep — best config per dataset (Hit Rate@10, seed=42, test_frac=0.2)
---------------------------------------------------------------------------------
  tools_gpu_50k / dev_tools (594 cases):
    BEST: --split temporal --bm25-weighting --rating-weighted-merge --neighbors 10
    Hit@10 = 0.3081  MRR = 0.0865  (vs default kNN random 0.2862 / 0.0825)
    Baselines (same temporal split): popularity 0.2997, random 0.2744

  industrial_dev_50k (168 cases, sparse):
    BEST: --bm25-weighting --rating-weighted-merge --neighbors 10  (random split)
    Hit@10 = 0.0536  MRR = 0.0130
    SVD-64 also competitive (0.0476); plain kNN default only 0.0179

  sample_clean (168 cases):
    BEST: --model svd --svd-components 64  (random split)
    Hit@10 = 0.0833  MRR = 0.0250
    kNN default 0.0476 — latent factors win on this slice

Takeaway: no single model wins everywhere. Use temporal+BM25+kNN for Tools;
try SVD on denser/clean slices; industrial needs more data or ALS/hybrid next.

Re-run sweeps:
  cd models && py -3 sweep_ranking.py
  cd models && py -3 sweep_ranking.py --out ../output/sweep_results_v2.csv

Iteration 09 — current best per dataset (Hit Rate@10)
-----------------------------------------------------
  tools_gpu_50k (594 cases):
    Hit@10 champion: ensemble temporal BM25+RW w=0.35  →  0.3283  (MRR 0.0917)
    MRR champion:    SVD temporal BM25+RW (96 comps)   →  0.3283 hit, MRR 0.0968

  dev_tools (594 cases) — NEW overall hit high:
    --split temporal --model svd --svd-components 96
    --bm25-weighting --rating-weighted-merge --neighbors 10
    Hit@10 = 0.3300  MRR = 0.0971

  industrial_dev_50k (168 cases):
    --model als --bm25-weighting  (random split)
    Hit@10 = 0.0655

  sample_clean (168 cases):
    --model svd --svd-components 64  (random split)
    Hit@10 = 0.0833

Full iteration write-ups: improvement_logs/
Quick iter08 sweep:  cd models && py -3 sweep_iteration08.py


---------------------------------------------------
Merging Neighbors => root@DESKTOP-HLH5OMC:/mnt/c/Users/ATM Rahat Hossain/Desktop/Capstone/models# python evaluate_ranking.py \
  --parquet "../output/tools_gpu_50k.parquet" \
  --top-k 10 --neighbors 5 --test-fraction 0.2 --seed 42
Scored neighbor merge: on | User-centered ratings: True
Cases evaluated: 594
Hit Rate@10: 0.2862
MRR@10: 0.0825

When Centered Users:
  --parquet "../output/tools_gpu_50k.parquet" \
  --top-k 10 --neighbors 5 --test-fraction 0.2 --seed 42 \
  --no-center-users
Scored neighbor merge: on | User-centered ratings: False
Cases evaluated: 594
Hit Rate@10: 0.2896
MRR@10: 0.0797
------------------------------------------------------