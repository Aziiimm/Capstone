# CPU path: data pipeline (out-of-core load, join, filter, Parquet)
# Install this for Tools and Home Improvement (CPU) or any CPU-only run.
dask[complete]>=2024.1.0
pyarrow>=14.0.0
pandas>=2.0.0

# Download from Hugging Face (optional, for download script)
huggingface_hub>=0.20.0

# Tests
pytest>=7.0.0

# GPU path (optional): only if you run the GPU pipeline (e.g. Electronics/Industrial GPU)
# pip install cudf-cu12 dask-cudf-cu12  # or match your CUDA version
