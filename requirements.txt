# CPU path: data pipeline (load, filter, Parquet) + CPU recommender (sklearn).
# Install: pip install -r requirements.txt

dask[complete]>=2024.1.0
pyarrow>=14.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
pytest>=7.0.0
